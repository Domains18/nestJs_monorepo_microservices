"use client"

// Importing Toaster from react-hot-toast library
import {Toaster} from "react-hot-toast"

// Exporting a ToasterProvider component
export const ToasterProvider = () => {
    // Rendering the Toaster component
    return <Toaster />
}